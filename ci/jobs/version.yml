version:
  extends: .runner-version
  stage: version
  image: dockerhub.devops.telekom.de/alpine@sha256:69665d02cb32192e52e07644d76bc6f25abeb5410edc1c7a81a10ba3f0efb90a
  script:
    - echo ${IMAGE_TAG}
    - echo ${IMAGE_TAG} > version
  artifacts:
    paths:
      - version
    expire_in: 1 week
  dependencies: []