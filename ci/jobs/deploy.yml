trigger-deploy:
  extends: .runner-deploy
  stage: deploy
  variables:
    IMAGE_TAG: ${CI_PIPELINE_IID}-${CI_COMMIT_REF_NAME}-${CI_COMMIT_SHORT_SHA}
  trigger:
    project: panamax/application/pbs
    strategy: depend